# Function Specifications

Every function in `src/` should include an immediately preceding spec comment with:

- General description
- Params
- Pre
- Post
- Mathematical definition where appropriate

Functions should remain narrow, compositional, and semantically obvious.
