# Datatype Specifications

Every datatype in `src/` should document:

- Abstract state
- Concrete state
- Representation invariants
- Abstraction function
- Notes on subtype and supertype clarity

Datatypes should represent one semantic concept each and avoid overloaded meaning.
